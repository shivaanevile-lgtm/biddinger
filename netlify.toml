[build]
  command = "npm install"
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# permanent results links: /r/ABCDEF is handled by the client
[[redirects]]
  from = "/r/*"
  to = "/index.html"
  status = 200
